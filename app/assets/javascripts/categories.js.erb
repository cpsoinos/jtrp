$(".categories.index").on('turbolinks:load', function() {

  var feed = new Instafeed({
    get: 'user',
    userId: '1026079620813320',
    accessToken: '<%= ENV['INSTAGRAM_ACCESS_TOKEN'] %>'
  });
  feed.run();

})
