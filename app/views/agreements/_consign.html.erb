<div class="text-right">
  <h4>Agreement No. <%= agreement.id %></h4>
</div>

<h1 class="text-center company-title"><%= @company.name %></h1>

<%= render 'agreements/consignment_terms', agreement: agreement %>

<h4>Addendums:</h4>

<div class="large-top-margin">
  <%= render 'items/items_list', items: items %>
</div>

<div class="large-top-margin">
  <%= render 'agreements/consignment_signature_blocks', agreement: agreement %>
</div>
