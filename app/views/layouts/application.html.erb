<!DOCTYPE html>
<html>
<head>
  <title>Jtrp</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  <%= javascript_include_tag 'application' %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= Gon::Base.render_data %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= controller_name %> <%= action_name %>">

  <div class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <% if @company %>
          <%= link_to @company.name, root_path, class: "navbar-brand" %>
        <% end %>
      </div>
      <div class="navbar-collapse collapse navbar-responsive-collapse">
        <% if current_user.try(:internal?) %>
          <ul class="nav navbar-nav">
            <li class="dropdown">
              <a href="<%= categories_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Categories
                <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <% if @categories %>
                  <% @categories.each do |category| %>
                  <li><%= link_to category.name, category_path(category) %></li>
                  <% end %>
                  <% end %>
                  <li class="divider"></li>
                  <li><%= link_to "All Categories", categories_path %></li>
                </ul>
              </li>
            <li class="dropdown">
              <a href="<%= items_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Items
                <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Potential", items_path(state: 'potential') %></li>
                <li><%= link_to "Active", items_path(state: 'active') %></li>
                <li><%= link_to "Sold", items_path(state: 'sold') %></li>
                <li class="divider"></li>
                <li><%= link_to "All Items", items_path %></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="<%= clients_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Clients
                <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Potential", clients_path(status: 'potential') %></li>
                <li><%= link_to "Active", clients_path(status: 'active') %></li>
                <li><%= link_to "Inactive", clients_path(status: 'inactive') %></li>
                <li class="divider"></li>
                <li><%= link_to "All Clients", clients_path %></li>
              </ul>
            </li>
          </ul>
        <% end %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'About Us', company_about_us_path(@company) %></li>
          <% if current_user %>
            <li><%= link_to 'Sign Out', destroy_user_session_path, method: 'delete' %></li>
          <% else %>
            <li><%= link_to 'Sign In', new_user_session_path %></li>
            <li><%= link_to 'Sign Up', new_user_registration_path %></li>
          <% end %>
          <!-- <li class="dropdown">
            <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Dropdown
              <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><a href="javascript:void(0)">Action</a></li>
              <li><a href="javascript:void(0)">Another action</a></li>
              <li><a href="javascript:void(0)">Something else here</a></li>
              <li class="divider"></li>
              <li><a href="javascript:void(0)">Separated link</a></li>
            </ul>
          </li> -->
        </ul>
        <!-- <form class="navbar-form navbar-right">
          <div class="form-group is-empty">
            <input type="text" class="form-control col-md-8" placeholder="Search">
            <span class="material-input"></span>
          </div>
        </form> -->
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-12">
        <% if alert %>
        <div class="alert alert-dismissible alert-warning">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <p><%= alert %></p>
        </div>
        <% end %>
        <% if notice %>
        <div class="alert alert-dismissible alert-success">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <p><%= notice %></p>
        </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>
