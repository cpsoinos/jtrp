<nav class="navbar navbar-inverse navbar-fixed-top hidden-print" role="navigation-demo">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if @company %>
        <div class="brand-logo-link">
          <span><%= link_to "<i class='icon-jtrpv2-allwhite-noname' style='font-size: 43pt'></i>".html_safe, root_path, class: "navbar-brand" %></span>
          <span><%= link_to @company.name, root_path, class: "navbar-brand" %></span>
        </div>
      <% end %>
    </div>

<!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <a href="<%= client_services_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown"><i class='material-icons'>view_list</i> Categories <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% if @categories %>
              <% @categories.each do |category| %>
                <li>
                  <%= link_to category.name, category_path(category) %>
                </li>
              <% end %>
            <% end %>
            <li>
              <%= link_to "All Categories", categories_path %>
            </li>
          </ul>
        </li>

        <% if current_user.try(:internal?) %>

          <li class="dropdown">
            <a href="<%= client_services_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown"><i class='material-icons'>weekend</i> Items <b class="caret"></b>
            </a>
            <ul class="dropdown-menu" id="items">
              <li><%= link_to "Potential", items_path(status: 'potential')%></li>
              <li><%= link_to "Active", items_path(status: 'active')%></li>
              <li><%= link_to "Sold", items_path(status: 'sold')%></li>
              <li><%= link_to "Inactive", items_path(status: 'inactive')%></li>
              <li><%= link_to "All Items", items_path%></li>
            </ul>
          </li>
          <li><%= link_to "<i class='material-icons'>assignment_ind</i> Accounts".html_safe, accounts_path %></li>
          <li><%= link_to "<i class='material-icons'>map</i> Jobs".html_safe, jobs_path %></li>

        <% end %>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="<%= client_services_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown"><i class='material-icons'>search</i>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= form_tag search_index_path, method: :get do %>
                <div class="input-group">
                  <div class="form-group is-empty" style="padding-left: 5px;">
                    <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Search" %>
                    <span class="material-input"></span>
                  </div>
                </div>
              <% end %>
            </li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="<%= client_services_path %>" data-target="#" class="dropdown-toggle" data-toggle="dropdown">About Us
            <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Client Services', client_services_path %></li>
            <li><%= link_to 'Consignment Policies', consignment_policies_path %></li>
            <li><%= link_to 'Service Rate Schedule', service_rate_schedule_path %></li>
            <li><%= link_to 'Service Rate Schedule for Agents', agent_service_rate_schedule_path %></li>
          </ul>
        </li>
        <% if current_user %>
          <li><%= link_to 'Sign Out', destroy_user_session_path, method: 'delete' %></li>
        <% else %>
          <li><%= link_to "Sign in with Clover", user_omniauth_authorize_path(:clover) %></li>
          <li><%= link_to 'Sign In', new_user_session_path %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-->
</nav>
