<table class="table table-striped table-hover">
  <tbody id="proposal-item-list">
    <% @proposal.items.order(:id).each do |item| %>
      <% next unless item.persisted? %>
      <%= render 'item_row', item: item %>
    <% end %>
  </tbody>
</table>
