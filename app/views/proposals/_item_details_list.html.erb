<div class="row grid">
  <div class="col-xs-12 col-md-6 grid-sizer"></div>
  <% items.order(:id).each do |item| %>
    <% next unless item.persisted? %>
    <%= render 'proposals/item_details_row', item: item %>
  <% end %>
</div>
